#include "StartrackerData.h"
#include <iostream>
#include <fstream>

using namespace std;

int main()
{
	/*
	cout << "starting test" << endl;
	MILWrapper MILWrapper1;
	
	// Logger:
	Logger myLogger("test log");
	myLogger.setlim(5);
	myLogger.setmaxlen(50 KB,SINGLEFILE);
	myLogger.open("/home/log/test.log");
	myLogger.lw(SPAM,"Starting test...");
	
	cout << "Configuring" << endl;
	cout << "Configure return: " << MILWrapper1.create(&myLogger) << endl;

	StartrackerImage stImg;
	double xfov = 0.125112182;
	double yfov = 0.105564494;
	unsigned short xlength = 512;
	unsigned short ylength = 512;
	MIL_ID sysID = MILWrapper1.SysID;
	
	cout << "Sysid: " << sysID << endl;
	cout << "create: " << stImg.create(sysID, xlength, ylength, xfov, yfov) << endl;
	*/
	
	return 0;
}
